@model Tuple<List<Core.Models.Device>, WebApplication5.Models.RoomDropDownViewModel>
    <input type="hidden" id="roomid" value="@Model.Item2.Id" />
<table class="table table-striped dataTables-example">
    <thead>
        <tr>
            <th>الجهاز</th>
            <th>IP </th>
            @*  <th>رقم الجهاز </th>*@
            @*<th>الغرفة </th>*@
            <th>اسم الشخص</th>
            <th>حالة الجهاز</th>
            @*<th>الخصائص </th>*@
        </tr>
    </thead>
    <tbody>

        @if (Model.Item1.Count() != 0)
        {
            foreach (var device in Model.Item1)
            {
        <tr class="newrow">
            <td id="deviceid" style="display:none">@device.deviceId</td>
            <td>  <img class="fixedimg2" src="../Content/img/5234658.png" height="37px" width="50px"></td>
            <td style="width:20%">
                <input class="form-control" id="IpAddress" value=" @device.ipAddress" />

            </td>
            @*<td>
                @device.roomName


            </td>*@
            <td>
                @*<select class="input-sm form-control input-s-sm inline" style="width:35%; height: 34px;line-height: 30px;font-size: 10px; float: right;font-weight: bold;">
                    <option value="0">بحث عن طريق</option>
                    <option value="1"> الاسم</option>
                    <option value="2"> البريد الألكتروني</option>
                    <option value="3"> رقم الجوال</option>
                    <option value="4"> الرقم الوظيفي</option>
                </select>*@

                <select class="input-sm form-control input-s-sm inline attendeeselect" style="height: 34px;line-height: 30px;font-size: 10px;float: right;font-weight: bold;width: 50%;margin-right: 10px;">
                    <option value="" selected>select</option>
                </select>
            </td>

            @if (device.isActive)
            {
            <td>
                مفعل
                

            </td>
            }
            else
            {
            <td>
                غير مفعل
            </td>
            }
            @*<td>
                @*   <a class="btn btn-info" href="javascript:" onclick="UpdateDevice()" dta-id="@device.deviceId">تسجيل</a>
                <a value="" onclick="ShowDeleteModal(@device.deviceId,'@Url.Action("Delete", "Device", new { id = device.deviceId })',this)"
                   class="btn btn-info btn-danger">حذف</a>
            </td>*@
        </tr>
            }
        }
     
    </tbody>
</table>
@*<div class="col-lg-12" style="padding:0">
    <a href="javascript:" onclick="AddDevice()" class="btn btn-success" style=" float:right"> <i class="fa fa-send"></i> <span class="bold">تسجيل</span></a>
</div>*@
